# Deploy Notes

- Deploy folder: `/Users/junoteen/Documents/New project/deployable-portfolio`
- Expires at: `2026-04-08 21:31:46 UTC+8`
- Home page links now point to:
  - `./brand/`
  - `./ui/`
  - `./visual/`

Manual publish option:

1. Open `https://app.netlify.com/drop`
2. Drag the whole `deployable-portfolio` folder into the drop zone
3. Share the generated `*.netlify.app` URL

After the expiry time, all pages will show an expiration notice automatically.
